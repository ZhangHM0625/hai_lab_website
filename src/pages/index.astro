---
/** * 【HAIL 实验室主页 - 全平台适配完整版】 
 * 开发者：Heming Zhang (RA)
 */
const labName = "HAIL";
const school = "WKWSCI"; 
const email = "HEMING001@e.ntu.edu.sg"; 

// 自动适配 GitHub Pages 子路径，解决 Logo 碎图问题
const logoImage = `${import.meta.env.BASE_URL}/ntu-logo.png`;

// 目前使用外部链接，如换成本地图片请使用 ${import.meta.env.BASE_URL}/your-image.jpg
const heroBgImage = "https://images.unsplash.com/photo-1451187580459-43490279c0fa?q=80&w=2000";

const affiliation = "ASIAN COMMUNICATION RESEARCH CENTRE (ACRC) | NTU";
const fullLabName = "Human AI-Interaction Lab";
const slogan = "Advancing Psychologically Aligned and Ethically Designed AI Systems for a Trustworthy Future of Human-AI Interaction.";
---

<html lang="zh-cn">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
<title>{labName} | NTU</title>
<style>
:root {
--ntu-red: #ce1126;
--text-dark: #0f172a;
--text-muted: #727c8a;
--text-hero-dim: #bbc5d0;
--bg-footer: #f8fafc;
--hero-height: 85vh;
--hero-gap-bottom: 20px;
--logo-display-height: 45px;
}

* { margin: 0; padding: 0; box-sizing: border-box; }
/* 开启全站平滑滚动 */
html { scroll-behavior: smooth; }

body {
font-family: 'Inter', system-ui, -apple-system, sans-serif;
background: #fff;
color: var(--text-dark);
/* 彻底杜绝移动端左右晃动 */
overflow-x: hidden;
width: 100%;
}

/* 1. 导航栏 (PC) */
.navbar {
display: flex;
justify-content: space-between;
align-items: center;
padding: 1rem 5%;
background: white;
position: sticky;
top: 0;
z-index: 1000;
border-bottom: 1px solid #f1f5f9;
}
.logo-module { display: flex; align-items: center; text-decoration: none; color: inherit; }
.university-logo { height: var(--logo-display-height); border-right: 1px solid #e2e8f0; padding-right: 15px; margin-right: 20px; }
.lab-info { display: flex; flex-direction: column; line-height: 1.1; }
.lab-info .main-title { font-weight: 800; font-size: 1.6rem; letter-spacing: 0px; }
.lab-info .sub-title { font-size: 0.8rem; color: var(--text-muted); font-weight: 600; text-transform: uppercase; }

.nav-links { display: flex; gap: 40px; list-style: none; }
.nav-links a { 
    text-decoration: none; 
    color: #1e293b; 
    font-weight: 600; 
    font-size: 1rem; 
    transition: all 0.3s ease; 
    letter-spacing: 0.5px;
    position: relative;
}
.nav-links a::after {
    content: '';
    position: absolute;
    bottom: -5px;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--ntu-red);
    transition: width 0.3s ease;
}
.nav-links a:hover {
    color: var(--ntu-red); 
    transform: translateY(-2px); 
}
.nav-links a:hover::after {
    width: 100%;
}

/* 2. Hero 区域 (PC) */
.hero-wrapper { 
    padding-bottom: var(--hero-gap-bottom); 
    background: #fff; 
    scroll-margin-top: 100px; 
}
.hero {
position: relative;
height: var(--hero-height);
width: 100%;
background-color: #050505;
display: flex;
align-items: center;
justify-content: flex-start;
color: white;
overflow: hidden;
}
.hero-bg {
position: absolute;
top: 0; left: 0; width: 100%; height: 100%;
z-index: 1;
background-size: cover;
background-position: center;
}

@keyframes fadeInUp {
    from { opacity: 0; transform: translateY(50px); }
    to { opacity: 1; transform: translateY(-20px); }
}

.hero-content { 
    position: relative; 
    z-index: 5; 
    width: 100%; 
    max-width: 1200px; 
    padding: 0 5%; 
    text-align: left;
    animation: fadeInUp 1.5s ease-out forwards;
}

.hero-affiliation { font-size: 1rem; font-weight: 700; margin-bottom: 1rem; letter-spacing: 0.05em; color: white; }
.hero-title { font-size: 3.5rem; font-weight: 800; line-height: 1.0; margin-bottom: 0.5rem; max-width: 800px; }
.hero-slogan { font-size: 1rem; color: var(--text-hero-dim); font-weight: 400; line-height: 1.4; max-width: 900px; }

.scroll-hint {
position: absolute;
bottom: 30px;
left: 50%;
transform: translateX(-50%);
z-index: 3;
text-decoration: none;
color: rgba(255, 255, 255, 0.5);
font-size: 1.2rem;
transition: 0.3s;
}
.scroll-hint:hover { color: white; transform: translateX(-50%) translateY(5px); }

/* 3. 内容区域 (PC) */
#content-section {
    scroll-margin-top: 80px; 
}

#research-focus {
max-width: 1200px;
margin: 80px auto 80px; 
padding: 0 5%;
text-align: center;
}
.area-title { 
    font-size: 2.2rem; 
    font-weight: 700; 
    margin-bottom: 70px; 
    text-transform: none; 
    letter-spacing: 0.5px; 
    color: var(--text-dark);
    position: relative;
}
.area-title::after {
    content: '';
    display: block;
    width: 80px;
    height: 3px;
    background: var(--ntu-red);
    margin: 15px auto 0;
    border-radius: 2px;
}
.research-flex { 
    display: flex; 
    flex-wrap: wrap; 
    justify-content: center; 
    gap: 30px;
}
.focus-card {
    flex: 0 1 calc(33.33% - 20px);
    min-width: 300px;
    background: #ffffff;
    padding: 45px 30px;
    border-radius: 16px;
    border: 1px solid #e2e8f0;
    box-shadow: 0 10px 30px rgba(0,0,0,0.03);
    transition: all 0.3s ease;
    text-align: left;
    position: relative;
    overflow: hidden;
}
.focus-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 4px;
    background: var(--ntu-red);
    transform: scaleX(0);
    transition: transform 0.3s ease;
}
.focus-card:hover {
    transform: translateY(-10px);
    border-color: #cbd5e1;
    box-shadow: 0 20px 40px rgba(0,0,0,0.08);
}
.focus-card:hover::before {
    transform: scaleX(1);
}
.focus-icon {
    font-size: 2.5rem;
    margin-bottom: 25px;
    display: inline-block;
    transition: transform 0.3s ease;
}
.focus-card:hover .focus-icon {
    transform: scale(1.1);
}
.focus-card h3 {
    font-size: 1.2rem;
    color: var(--text-dark);
    line-height: 1.3;
    font-weight: 700;
    margin-bottom: 15px;
    letter-spacing: 0.5px;
}
.focus-card p {
    font-size: 0.95rem;
    color: var(--text-muted);
    line-height: 1.6;
    margin: 0;
}

/* 4. Footer */
.footer { padding: 40px 5% 30px 5%; text-align: center; background: var(--bg-footer); border-top: 1px solid #f1f5f9; }
.footer-text { font-size: 0.85rem; color: var(--text-muted); margin-bottom: 10px; line-height: 1.3; }
.copy-btn { margin-top: 12px; padding: 10px 32px; background: #1e293b; color: white; border-radius: 40px; font-size: 0.85rem; font-weight: 500; cursor: pointer; border: none; transition: all 0.3s ease; }
.copy-btn:hover { background: var(--ntu-red); transform: scale(1.05); }

/* --- 5. 手机端专项适配修复 (Responsive Mix) --- */
@media (max-width: 768px) {
  body, html { overflow-x: hidden; width: 100%; position: relative; }

  .navbar { padding: 1rem 3%; }
  .university-logo { height: 30px; padding-right: 8px; margin-right: 8px; }
  .lab-info .main-title { font-size: 1rem; font-weight: 700; }
  .nav-links { gap: 15px; }
  .nav-links a { 
    font-size: 0.75rem; 
    white-space: nowrap; 
    font-weight: 500;
  }
  .nav-links a::after {
    bottom: -3px;
    height: 1px;
  }

  /* 保持横版视觉比例的 vw 单位缩放 */
  .hero { height: 75vh; min-height: 450px; }
  .hero-content { padding: 0 6%; transform: translateY(-10px); }
  .hero-affiliation { font-size: 3.0vw; margin-bottom: 1rem; }
  .hero-title { font-size: 9vw; line-height: 1.1; max-width: 95%; margin-bottom: 0.8rem; }
  .hero-slogan { font-size: 3.0vw; line-height: 1.2; max-width: 95%; color: var(--text-hero-dim); }

  /* 调整卡片比例，使其在移动端也美观 */
  .area-title { 
    font-size: 6vw; 
    margin-bottom: 8vw; 
  }
  .area-title::after {
    width: 60px;
    height: 2px;
    margin: 10px auto 0;
  }
  #research-focus { 
    margin: 12vw auto; 
    padding: 0 5%; 
  }
  .research-flex {
    gap: 4vw;
  }
  .focus-card {
    flex: 0 1 95%; 
    padding: 6vw 5vw; 
    margin-bottom: 0; 
    min-height: auto; 
    display: block;
    border-radius: 12px;
  }
  .focus-icon {
    font-size: 8vw;
    margin-bottom: 4vw;
  }
  .focus-card h3 { 
    font-size: 5vw; 
    line-height: 1.4; 
    margin-bottom: 3vw;
  }
  .focus-card p {
    font-size: 4vw;
    line-height: 1.5;
  }
}
</style>
</head>
<body>
<nav class="navbar">
<a href={`${import.meta.env.BASE_URL}/#home`} class="logo-module">
<img src={logoImage} alt="NTU Logo" class="university-logo" />
<div class="lab-info">
<span class="main-title">{labName}</span>
<span class="sub-title">{school}</span>
</div>
</a>
<ul class="nav-links">
<li><a href={`${import.meta.env.BASE_URL}/#home`}>Home</a></li>
<li><a href="#content-section">Research</a></li>
<li><a href="/publications">Publications</a></li>
<li><a href="/people">People</a></li>
</ul>
</nav>

<div id="home" class="hero-wrapper">
<header class="hero">
<div class="hero-bg" style={`background-image: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.8)), url(${heroBgImage});`}></div>
<div class="hero-content">
<p class="hero-affiliation">{affiliation}</p>
<h1 class="hero-title">{fullLabName}</h1>
<p class="hero-slogan">{slogan}</p>
</div>
<a href="#content-section" class="scroll-hint">↓</a>
</header>
</div>

<div id="content-section">
<main id="research-focus">
<h2 class="area-title">Research Focus</h2>
<div class="research-flex">
    <div class="focus-card">
        <div class="focus-icon">🎯</div>
        <h3>AI Agent Design & User Experience</h3>
        <p>Creating intelligent agents with intuitive interfaces and seamless user interactions</p>
    </div>
    <div class="focus-card">
        <div class="focus-icon">🔬</div>
        <h3>AI-Enhanced Research Methods</h3>
        <p>Leveraging AI to advance scientific inquiry and methodology</p>
    </div>
    <div class="focus-card">
        <div class="focus-icon">💡</div>
        <h3>Explainable AI & User Understanding</h3>
        <p>Developing transparent AI systems that users can trust and comprehend</p>
    </div>
    <div class="focus-card">
        <div class="focus-icon">🌐</div>
        <h3>Immersive AI Experiences</h3>
        <p>Building immersive environments powered by artificial intelligence</p>
    </div>
    <div class="focus-card">
        <div class="focus-icon">⚖️</div>
        <h3>AI Ethics, Philosophy & Policy</h3>
        <p>Exploring ethical frameworks and policy implications of AI technologies</p>
    </div>
</div>
</main>

<footer class="footer">
<p class="footer-text">Thoughts? Ideas? Collaboration opportunities?</p>
<p class="footer-text">Get in touch with us</p>
<button class="copy-btn" id="copyEmail" data-email={email}>Email Us</button>
</footer>
</div>

<script is:inline>
document.getElementById('copyEmail').addEventListener('click', function() {
const email = this.getAttribute('data-email');
navigator.clipboard.writeText(email).then(() => {
const originalText = this.innerText;
this.innerText = 'Copied!';
this.style.background = '#ce1126';
setTimeout(() => {
this.innerText = 'Email Us';
this.style.background = '#1e293b';
}, 2000);
});
});
</script>
</body>
</html>